# Server Configuration
PORT=3001
NODE_ENV=development

# Campaigns Directory
CAMPAIGNS_DIR=./campaigns

# Player Sites Directory
PLAYER_SITES_DIR=./player-sites

# Google Drive Sync (optional)
GOOGLE_DRIVE_ENABLED=false
GOOGLE_DRIVE_CREDENTIALS=

# Anthropic Claude API (for NPC generation)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# =============================================================================
# Security Settings
# =============================================================================

# Authentication Passwords
# Set these to enable password protection for your campaign hub.
# Without these, anyone on your local network can access all features.
# Use different passwords for DM (full access) and Player (read-only) modes.
DM_PASSWORD=
PLAYER_PASSWORD=

# Rate Limiting
# Maximum API requests per 15-minute window (default: 100)
RATE_LIMIT_MAX=100
